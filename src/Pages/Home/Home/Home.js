import React from 'react';
import Carousel from '../Carousel/Carousel';

const Home = () => {
    return (
        <div>
            <Carousel></Carousel>
        </div>
    );
};

export default Home;